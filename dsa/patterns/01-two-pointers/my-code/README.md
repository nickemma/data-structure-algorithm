# My code — Pattern 01

Paste your **passing** LeetCode submissions here. Not your drafts — the version that went green.

Why bother: in three weeks you'll re-solve these from scratch, and you'll want to diff what you write then against what you wrote today. That diff is the only honest measure of whether this is working.

## Log

Fill this in as you go. `Solved in` is the wall-clock time from reading the prompt to the green tick, including the times you got stuck.

| Problem | Language | Solved in | Hints used | Passed 1st submit? | What went wrong |
| --- | --- | --- | --- | --- | --- |
| 167 Two Sum II | | | | | |
| 11 Container With Most Water | | | | | |

## Files

- `python/two_sum_ii.py` · `typescript/twoSumII.ts`
- `python/container_with_most_water.py` · `typescript/containerWithMostWater.ts`

Each stub has a `TODO`. Replace it with your submission, keep the test block at the bottom, and run it locally before you tell me you're done:

```bash
python3 python/two_sum_ii.py
node typescript/twoSumII.ts     # Node 22+ runs .ts directly, no install needed
```

## After it passes — write the retro

Three lines at the bottom of your file, in a comment. This is the part that makes it stick:

```
# Pattern:    two pointers, converging
# Why it fit: sorted input + find a pair + O(1) space required
# Cost me:    forgot the array was 1-indexed, failed submit #1
```

If you can't fill in "why it fit" without looking it up, you got the answer but not the lesson — re-read [../README.md](../README.md) section 3.
